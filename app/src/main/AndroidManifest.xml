<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nassaty.hireme">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.nassaty.hireme.activities.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.Material" />
        <activity android:name="com.nassaty.hireme.activities.AddNewJob" />
        <activity android:name="com.nassaty.hireme.activities.Favorites" />
        <activity android:name="com.nassaty.hireme.activities.AuthActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.nassaty.hireme.activities.AppList" />
        <activity android:name="com.nassaty.hireme.activities.Register" />
        <activity android:name="com.nassaty.hireme.activities.Profile">

        </activity>
    </application>

</manifest>